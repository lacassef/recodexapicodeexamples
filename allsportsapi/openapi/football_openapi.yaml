openapi: 3.0.0
info:
  title: Football API
  version: 1.0.0
  description: OpenAPI specification generated from SofaScore route annotations.
servers:
  - url: http://localhost:8080
    description: Local development server
paths:
  /api/tv/country/all/event/{id}:
    get:
      summary: Get match TV countries
      description: Lists the countries and the TV channels within them that are broadcasting a specific match.
      tags:
        - AllSports
        - TV
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the match
          example: 14081808
      responses:
        '200':
          description: A JSON object listing countries and their broadcasting channels.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/channel/{channId}/event/{id}:
    get:
      summary: Get match TV channel votes
      description: Fetches viewer voting data from a specific TV channel that is broadcasting the match.
      tags:
        - AllSports
        - TV
      parameters:
        - name: channId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the TV channel
          example: 3177
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the match
          example: 10974920
      responses:
        '200':
          description: A JSON object containing viewer vote data from the channel.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/img/flag/{flag}:
    get:
      summary: Get country flag
      description: Fetches the PNG flag image asset corresponding to a provided country code. The response is the raw 
        image data.
      tags:
        - AllSports
      parameters:
        - name: flag
          in: path
          required: true
          schema:
            type: string
          description: Country code or identifier
          example: jp
      responses:
        '200':
          description: The country's flag image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/country/{code}/flag:
    get:
      summary: Get country flag (img API)
      description: Fetches the flag image from the SofaScore image API by country code.
      tags:
        - AllSports
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          description: Country code or identifier
          example: AU
      responses:
        '200':
          description: The country's flag image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/tournament.png:
    get:
      summary: Get tournament placeholder image
      description: Returns a static placeholder graphic for a tournament. The response is an SVG image.
      tags:
        - AllSports
        - Tournaments
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/tournament.svg:
    get:
      summary: Get tournament placeholder image
      description: Returns a static placeholder graphic for a tournament. The response is an SVG image.
      tags:
        - AllSports
        - Tournaments
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/team.svg:
    get:
      summary: Get team placeholder image
      description: Returns a static placeholder graphic for a team. The response is an SVG image.
      tags:
        - AllSports
        - Teams
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/player.png:
    get:
      summary: Get player placeholder image
      description: Returns a static placeholder graphic for a player. The response is an SVG image.
      tags:
        - AllSports
        - Players
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/player.svg:
    get:
      summary: Get player placeholder image
      description: Returns a static placeholder graphic for a player. The response is an SVG image.
      tags:
        - AllSports
        - Players
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/manager.png:
    get:
      summary: Get manager placeholder image
      description: Returns a static placeholder graphic for a manager. The response is an SVG image.
      tags:
        - AllSports
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/manager.svg:
    get:
      summary: Get manager placeholder image
      description: Returns a static placeholder graphic for a manager. The response is an SVG image.
      tags:
        - AllSports
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/calendar/season/{seasonId}/{year}/days-with-events:
    get:
      summary: Get calendar days with events
      description: Retrieves days that have events for a specific season and year.
      tags:
        - Sofascore
        - Calendar
      parameters:
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Calendar year
          example: 2026
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/category/{categoryId}/image:
    get:
      summary: Get category image
      description: Retrieves the image/logo for a sport category.
      tags:
        - Sofascore
        - Categories
      parameters:
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
          description: Sport category identifier
          example: 1
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/countries/all:
    get:
      summary: Get all countries
      description: Retrieves a list of all available countries.
      tags:
        - Sofascore
        - Config
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/rankings/team/{teamId}:
    get:
      summary: Get team rankings
      description: Retrieves rankings for a specific team.
      tags:
        - Sofascore
        - Rankings
      parameters:
        - name: teamId
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/all:
    get:
      summary: Search all entities
      description: Searches across all entity types (teams, players, tournaments).
      tags:
        - Sofascore
        - Search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/suggestions/{entityType}:
    get:
      summary: Get search suggestions
      description: Retrieves search suggestions for a specific entity type.
      tags:
        - Sofascore
        - Search
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
          description: Entity type (teams, players, events, tournaments)
          example: teams
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/unique-tournaments:
    get:
      summary: Search unique tournaments
      description: Searches for unique tournaments.
      tags:
        - Sofascore
        - Search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/{entityType}:
    get:
      summary: Search by entity type
      description: Searches for entities of a specific type.
      tags:
        - Sofascore
        - Search
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
          description: Entity type (teams, players, events, tournaments)
          example: teams
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/channel/{id}/schedule:
    get:
      summary: Get TV channel schedule
      description: Retrieves the broadcast schedule for a specific TV channel.
      tags:
        - Sofascore
        - TV
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: TV channel identifier
          example: 3177
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/country/{countryCode}/channels:
    get:
      summary: Get country TV channels
      description: Retrieves available TV channels for a specific country.
      tags:
        - Sofascore
        - TV
      parameters:
        - name: countryCode
          in: path
          required: true
          schema:
            type: string
          description: ISO 3166-1 alpha-2 country code
          example: MZ
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/{term}:
    get:
      summary: Search football entities
      description: Searches for football teams, players, and tournaments. The `type` parameter in the upstream URL is 
        hardcoded to `all`.
      tags:
        - Football
        - Search
      parameters:
        - name: term
          in: path
          required: true
          schema:
            type: string
          description: Search term
          example: champions
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: Page number for pagination (defaults to 0)
          example: 0
      responses:
        '200':
          description: A JSON object containing search results.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid request parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/matches/{day}/{month}/{year}:
    get:
      summary: Get all football matches by date
      description: Fetches all football matches scheduled for a specific date by combining results from two upstream 
        endpoints (regular and inverse).
      tags:
        - Football
        - Matches
      parameters:
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 21
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A JSON object containing a list of matches for the specified date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/matches/live:
    get:
      summary: Get live football matches
      description: Retrieves all currently live football matches.
      tags:
        - Football
        - Matches
      responses:
        '200':
          description: A JSON object containing a list of live matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/matches/top/{day}/{month}/{year}:
    get:
      summary: Get top football matches by date
      description: Retrieves a curated list of top-tier or more important football matches for a specific date.
      tags:
        - Football
        - Matches
      parameters:
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 21
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A JSON object containing a list of top matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/matches/bottom/{day}/{month}/{year}:
    get:
      summary: Get bottom football matches by date
      description: Retrieves a list of less prominent or lower-tier football matches for a specific date.
      tags:
        - Football
        - Matches
      parameters:
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 21
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A JSON object containing a list of bottom-tier matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/categories:
    get:
      summary: Get football tournament categories
      description: Retrieves a list of all available football tournament categories, typically corresponding to 
        countries or regions.
      tags:
        - Football
        - Tournaments
      responses:
        '200':
          description: A JSON object containing a list of tournament categories.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/all/category/{id}:
    get:
      summary: Get tournaments by category
      description: Retrieves all unique tournaments within a specific category ID (e.g., a country).
      tags:
        - Football
        - Categories
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Category ID
          example: 1
      responses:
        '200':
          description: A JSON object containing a list of unique tournaments for the category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/category/{id}/events/{day}/{month}/{year}:
    get:
      summary: Get matches by category and date
      description: Retrieves all football matches for a specific category on a given date.
      tags:
        - Football
        - Categories
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Category ID
          example: 1
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 21
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A JSON object containing a list of scheduled matches for the category on the given date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/meta:
    get:
      summary: Get tournament metadata
      description: Retrieves SEO metadata and other information for a specific tournament.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 16
      responses:
        '200':
          description: A JSON object containing metadata for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/rankings/fifa:
    get:
      summary: Get FIFA rankings
      description: Retrieves the current official FIFA national team rankings.
      tags:
        - Football
        - Rankings
      responses:
        '200':
          description: A JSON object containing the list of FIFA rankings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/rankings/uefa/countries:
    get:
      summary: Get UEFA countries rankings
      description: Retrieves the current UEFA coefficient rankings for national associations.
      tags:
        - Football
        - Rankings
      responses:
        '200':
          description: A JSON object containing the list of UEFA country rankings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/rankings/uefa/clubs:
    get:
      summary: Get UEFA clubs rankings
      description: Retrieves the current UEFA coefficient rankings for club teams.
      tags:
        - Football
        - Rankings
      responses:
        '200':
          description: A JSON object containing the list of UEFA club rankings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}:
    get:
      summary: Get football match details
      description: Retrieves detailed information for a specific football match by its ID, including teams, score, 
        status, and tournament info.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing detailed information about the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/statistics:
    get:
      summary: Get match statistics
      description: Retrieves comprehensive team and player statistics for a specific match, such as possession, shots, 
        and passes.
      tags:
        - Football
        - Matches
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 13472836
      responses:
        '200':
          description: A JSON object containing comprehensive statistics for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/incidents:
    get:
      summary: Get match incidents
      description: Retrieves a chronological list of all significant incidents from a match, such as goals, cards, and 
        substitutions.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing a list of incidents from the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{customId}/h2h:
    get:
      summary: Get head-to-head history
      description: Retrieves the detailed match history for head-to-head encounters between the two teams in a specific 
        match.
      tags:
        - Football
        - Matches
        - Statistics
      parameters:
        - name: customId
          in: path
          required: true
          schema:
            type: string
          description: A custom identifier for the match, used for H2H lookups
          example: ugbszgb
      responses:
        '200':
          description: A JSON object containing a detailed list of past H2H matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/duel:
    get:
      summary: Get head-to-head summary
      description: Retrieves a summary of head-to-head statistics, including win/draw/loss counts and recent form.
      tags:
        - Football
        - Matches
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing a summary of H2H statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/form:
    get:
      summary: Get match form analysis
      description: Retrieves pre-game form, including recent results (e.g., WWDLW) and key stats for both teams.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing pre-game form and performance trends.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/odds:
    get:
      summary: Get match betting odds
      description: Retrieves available betting odds from bookmaker `1` for a specific match, including various markets.
      tags:
        - Football
        - Matches
        - Odds
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14217375
      responses:
        '200':
          description: A JSON object containing a list of available betting odds.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/odds/{providerId}/featured:
    get:
      summary: Get match featured odds
      description: Retrieves featured odds for a specific match and provider.
      tags:
        - Football
        - Matches
        - Odds
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14054688
        - name: providerId
          in: path
          required: true
          schema:
            type: integer
          description: Provider identifier
          example: 1
      responses:
        '200':
          description: Featured odds for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/provider/{providerId}/winning-odds:
    get:
      summary: Get match winning odds
      description: Retrieves winning odds for a specific match and provider.
      tags:
        - Football
        - Matches
        - Odds
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
        - name: providerId
          in: path
          required: true
          schema:
            type: integer
          description: Provider identifier
          example: 1
      responses:
        '200':
          description: Winning odds for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/shotmap:
    get:
      summary: Get match shot map
      description: Retrieves the shot map for a match, showing the location and outcome of every shot taken.
      tags:
        - Football
        - Matches
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14015387
      responses:
        '200':
          description: A JSON object containing shot map data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/graph:
    get:
      summary: Get match momentum graph
      description: Retrieves data points for the match momentum graph, visualizing team pressure and scoring 
        progression.
      tags:
        - Football
        - Matches
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing data points for the match momentum graph.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/lineups:
    get:
      summary: Get match lineups
      description: Retrieves starting lineups, formations, substitutes, and coach details for both teams.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14081808
      responses:
        '200':
          description: A JSON object containing starting lineups and substitutes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/win-probability:
    get:
      summary: Get match win probability
      description: Retrieves real-time win, draw, and loss probabilities at different points during the match.
      tags:
        - Football
        - Matches
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14081808
      responses:
        '200':
          description: A JSON object containing win probability data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/{playerId}/statistics:
    get:
      summary: Get player match statistics
      description: Fetches detailed performance data for a single player within a specific match, including a statistics
        summary and heatmap.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 13295530
        - name: playerId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 126774
      responses:
        '200':
          description: A JSON object containing detailed statistics for the player in the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/{playerId}/rating-breakdown:
    get:
      summary: Get match player rating breakdown
      description: Returns the component breakdown that forms a player's SofaScore rating for a given match.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 13424932
        - name: playerId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 991421
      responses:
        '200':
          description: A JSON object detailing the rating components.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/{playerId}/shotmap:
    get:
      summary: Get football match player shotmap
      description: Retrieves the shot map for a single player in a match, showing shot locations and outcomes.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 13295530
        - name: playerId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 126774
      responses:
        '200':
          description: A JSON object containing the player's shot map data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/{playerId}/heatmap:
    get:
      summary: Get match player heatmap
      description: Provides positional heatmap data showing areas of on-field activity for a specific player during a 
        match.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14012306
        - name: playerId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 985186
      responses:
        '200':
          description: A JSON object containing the player's heatmap data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/average-positions:
    get:
      summary: Get match average positions
      description: Provides positional heatmap data showing the average on-field position for all players during a 
        match.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 14015387
      responses:
        '200':
          description: A JSON object containing average position data for both teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/highlights:
    get:
      summary: Get match highlights
      description: Retrieves a list of video highlight clips and related metadata for a specific match.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing highlight clips and metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/managers:
    get:
      summary: Get match managers
      description: Returns information about the managers (coaches) for both teams in a specified match.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing details for both managers.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/votes:
    get:
      summary: Get match votes
      description: Returns fan poll percentages and vote totals for the match outcome (home win, draw, away win).
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing vote counts and percentages.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/best-players:
    get:
      summary: Get match best players
      description: Returns ratings and key statistics for the standout players in a match.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 10114139
      responses:
        '200':
          description: A JSON object containing a list of the best performing players.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/streaks:
    get:
      summary: Get team streaks
      description: Provides recent form and streak information (e.g., unbeaten runs, winless streaks) for each team in a
        match.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14015387
      responses:
        '200':
          description: A JSON object containing streak information for both teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/streaks/odds:
    get:
      summary: Get match streaks with odds
      description: Returns recent team streaks combined with associated betting odds from bookmaker `1`.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14030789
      responses:
        '200':
          description: A JSON object containing team streaks and betting odds.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/team/{teamId}/heatmap:
    get:
      summary: Get match team heatmap by ID
      description: Provides an aggregated positional data heatmap for an entire team in a match, specified by its ID.
      tags:
        - Football
        - Matches
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 13295530
        - name: teamId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the team
          example: 1769
      responses:
        '200':
          description: A JSON object containing the team's heatmap data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/commentary:
    get:
      summary: Get match commentary
      description: Fetches a live text commentary feed with entries for key moments in the specified match.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the match
          example: 14081808
      responses:
        '200':
          description: A JSON object containing a list of commentary entries.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/players/featured:
    get:
      summary: Get match featured players
      description: Retrieves a list of players highlighted by SofaScore for the specified match.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 13472836
      responses:
        '200':
          description: A JSON object containing a list of featured players.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{eventId}/best-players/summary:
    get:
      summary: Get match best-player summary
      description: Returns a summary of the best player's performance, highlighting standout statistics.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: eventId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 13472836
      responses:
        '200':
          description: A JSON object containing a summary of the best player's performance.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/meta:
    get:
      summary: Get event metadata
      description: Retrieves SEO and metadata for a specific event.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique match/event identifier
          example: 14081808
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/odds/{providerId}/all:
    get:
      summary: Get all odds for event
      description: Retrieves all available odds from a specific provider for an event.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique match/event identifier
          example: 14081808
        - name: providerId
          in: path
          required: true
          schema:
            type: integer
          description: Odds provider identifier
          example: 1
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/weather:
    get:
      summary: Get event weather
      description: Retrieves weather conditions for an outdoor event.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique match/event identifier
          example: 14081808
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/graph/sequence:
    get:
      summary: Get event graph sequence
      description: Retrieves the momentum/pressure graph sequence data for a match.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique match/event identifier
          example: 14081808
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/official-tweets:
    get:
      summary: Get event official tweets
      description: Retrieves official social media posts related to an event.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique match/event identifier
          example: 14081808
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}:
    get:
      summary: Get team details
      description: Fetches comprehensive details for a specific team, including name, country, venue, and tournament 
        information.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 44
      responses:
        '200':
          description: A JSON object containing detailed information about the team.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/achievements:
    get:
      summary: Get team achievements
      description: Returns the achievements and trophies associated with the specified team.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 2829
      responses:
        '200':
          description: A JSON object containing team achievements.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/image:
    get:
      summary: Get team logo
      description: Downloads the official logo (crest) for the specified team ID. The response is the raw image data.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the team
          example: 24798
      responses:
        '200':
          description: The team's logo image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/image/small:
    get:
      summary: Get team small logo
      description: Downloads the small logo (crest) for the specified team ID. The response is the raw image data.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the team
          example: 6117
      responses:
        '200':
          description: The team's small logo image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/players:
    get:
      summary: Get team squad
      description: Returns the current roster of players (squad) associated with the specified team.
      tags:
        - Football
        - Teams
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 44
      responses:
        '200':
          description: A JSON object containing the team's current player roster.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/media:
    get:
      summary: Get team media
      description: Returns a collection of media items (images, videos) related to the specified team.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 38
      responses:
        '200':
          description: A JSON object containing a list of media items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/matches/near:
    get:
      summary: Get team near matches
      description: Returns a list of recent and upcoming matches for the specified team, centered around the current 
        date.
      tags:
        - Football
        - Matches
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 44
      responses:
        '200':
          description: A JSON object containing a list of nearby matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/matches/previous/{page}:
    get:
      summary: Get previous team matches
      description: Fetches a paginated list of completed matches for a team, sorted by most recent first.
      tags:
        - Football
        - Matches
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 5153
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the team's previous matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/matches/next/{page}:
    get:
      summary: Get upcoming team matches
      description: Fetches a paginated list of scheduled future matches for a team, sorted chronologically.
      tags:
        - Football
        - Matches
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 5153
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the team's upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/players/statistics/seasons:
    get:
      summary: Get team player statistics seasons
      description: Returns a list of seasons for which player-level statistics are available for the specified team.
      tags:
        - Football
        - Teams
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 38
      responses:
        '200':
          description: A JSON object containing a list of seasons with available player statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/statistics/seasons:
    get:
      summary: Get team statistics seasons
      description: Provides a list of seasons for which aggregated team-level statistics are available.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 292595
      responses:
        '200':
          description: A JSON object containing a list of seasons with available team statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/tournament/{tournamentId}/season/{seasonId}/statistics:
    get:
      summary: Get team overall statistics
      description: Returns aggregated statistics for a team within a specific tournament and season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 292595
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 11541
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 69831
      responses:
        '200':
          description: A JSON object containing the team's overall statistics for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/tournament/{tournamentId}/season/{seasonId}/best-players:
    get:
      summary: Get team top players overall
      description: Returns a list of the top-performing players for a team in a specific tournament and season.
      tags:
        - Football
        - Teams
        - Players
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 38
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 357
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 69619
      responses:
        '200':
          description: A JSON object containing a list of the team's top players.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/tournaments:
    get:
      summary: Get team unique tournaments
      description: Enumerates all unique competitions a team has participated in.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 44
      responses:
        '200':
          description: A JSON object containing a list of unique tournaments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/transfers:
    get:
      summary: Get team transfer history
      description: Returns a chronological list of a team's incoming and outgoing player transfers.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 44
      responses:
        '200':
          description: A JSON object containing lists of players transferred in and out.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/standings/seasons:
    get:
      summary: Get team standings seasons
      description: Retrieves a list of seasons for which standings information is available for the team.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the team
          example: 38
      responses:
        '200':
          description: A JSON object containing a list of seasons with available standings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{teamId}/tournament/{tournamentId}/season/{seasonId}/goal-distributions:
    get:
      summary: Get team goal distributions
      description: Provides goal distribution data (goals scored/conceded in different time intervals) for a team in a 
        tournament season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: teamId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the team
          example: 5153
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 210
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76909
      responses:
        '200':
          description: A JSON object containing goal distribution statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/team/{teamId}/performance:
    get:
      summary: Get team league performance graph
      description: Returns time-series data illustrating a team's league position and performance throughout a 
        tournament season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 40
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 69956
        - name: teamId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the team
          example: 1769
      responses:
        '200':
          description: A JSON object containing data points for the team's performance graph.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/featured-event:
    get:
      summary: Get team featured event
      description: Retrieves the current or next featured event for a team.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/featured-players:
    get:
      summary: Get team featured players
      description: Retrieves highlighted/featured players for a team.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/unique-tournaments/all:
    get:
      summary: Get all team tournaments
      description: Retrieves all unique tournaments a team participates in.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/team/{id}/tournament/{tournamentId}/season/{seasonId}/ranks/overall:
    get:
      summary: Get team overall ranks
      description: Retrieves overall ranking data for a team in a tournament season.
      tags:
        - Football
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}:
    get:
      summary: Get player details
      description: Fetches profile data for a specific player, such as name, team, nationality, and position.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 103417
      responses:
        '200':
          description: A JSON object containing detailed information about the player.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/image:
    get:
      summary: Get player image
      description: Downloads the profile picture for the specified player ID. The response is the raw image data.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 985186
      responses:
        '200':
          description: The player's profile image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/media:
    get:
      summary: Get player media
      description: Retrieves a collection of media items (images, videos) related to the specified player.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the player
          example: 920546
      responses:
        '200':
          description: A JSON object containing a list of media items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/statistics/national:
    get:
      summary: Get player national team statistics
      description: Returns performance data and statistics for a player's appearances with their national team.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 982780
      responses:
        '200':
          description: A JSON object containing the player's national team statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/summary:
    get:
      summary: Get player last year summary
      description: Fetches aggregated performance metrics for a player from the previous year.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 103417
      responses:
        '200':
          description: A JSON object containing a summary of the player's performance last year.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/matches/near:
    get:
      summary: Get player nearby matches
      description: Returns a list of the player's recent and upcoming matches.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 3041
      responses:
        '200':
          description: A JSON object containing a list of nearby matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/statistics/season:
    get:
      summary: Get player seasonal statistics
      description: Fetches a list of seasons for which aggregated statistics are available for a player.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 982780
      responses:
        '200':
          description: A JSON object containing a list of seasons with available statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/tournament/{tournamentId}/season/{seasonId}/last-ratings:
    get:
      summary: Get player last ratings
      description: Fetches the most recent SofaScore ratings for a player in a specific tournament season.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 3041
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 7
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 36886
      responses:
        '200':
          description: A JSON object containing the player's last ratings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/tournament/{tournamentId}/season/{seasonId}/ratings:
    get:
      summary: Get player ratings
      description: Returns the historical SofaScore rating history for a player in a specific tournament season.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 982780
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 357
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 69619
      responses:
        '200':
          description: A JSON object containing the player's rating history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/tournament/{tournamentId}/season/{seasonId}/statistics:
    get:
      summary: Get player overall statistics
      description: Returns aggregated performance metrics for a player across all phases (e.g., regular season, 
        playoffs) of a tournament season.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the player
          example: 982780
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 357
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 69619
      responses:
        '200':
          description: A JSON object containing the player's overall statistics for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/characteristics:
    get:
      summary: Get player characteristics
      description: Returns a list of a player's noted strengths and weaknesses based on statistical analysis.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 982780
      responses:
        '200':
          description: A JSON object containing the player's characteristics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/attribute:
    get:
      summary: Get player attributes
      description: Returns radar-chart attribute scores (e.g., attacking, defending) and other skill ratings for a 
        player.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the player
          example: 982780
      responses:
        '200':
          description: A JSON object containing the player's attribute scores.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/transfer:
    get:
      summary: Get player transfer history
      description: Returns a chronological list of a player's transfers between clubs.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 103417
      responses:
        '200':
          description: A JSON object containing the player's transfer history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/penalty:
    get:
      summary: Get player penalty history
      description: Returns information about a player's history of taking penalty kicks.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 24055
      responses:
        '200':
          description: A JSON object containing the player's penalty history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/matches/previous/{page}:
    get:
      summary: Get player match history
      description: Fetches a paginated list of completed matches a player has participated in, sorted by most recent 
        first.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the player
          example: 982780
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the player's match history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/tournament/{tournamentId}/season/{seasonId}/heatmap:
    get:
      summary: Get player season heatmap overall
      description: Fetches a player's combined positional data heatmap for a specific tournament season.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the player
          example: 982780
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 357
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 69619
      responses:
        '200':
          description: A JSON object containing the player's overall heatmap for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/penalty-history/tournament/{tournamentId}/season/{seasonId}:
    get:
      summary: Get player penalty history by season
      description: Provides a player's historical penalty kick data within a specific tournament and season.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the player
          example: 831005
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 61643
      responses:
        '200':
          description: A JSON object containing the player's penalty history for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/statistics/all:
    get:
      summary: Get all player statistics
      description: Retrieves comprehensive statistics for a player across all competitions.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique player identifier
          example: 831005
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/statistics/match-type/{matchType}:
    get:
      summary: Get player statistics by match type
      description: Retrieves player statistics filtered by match type (home/away/overall).
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique player identifier
          example: 831005
        - name: matchType
          in: path
          required: true
          schema:
            type: string
          description: Match type filter (home, away, overall)
          example: overall
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/media/videos:
    get:
      summary: Get player media videos
      description: Retrieves video highlights and media for a player.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique player identifier
          example: 831005
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/unique-tournaments:
    get:
      summary: Get player tournaments
      description: Retrieves all unique tournaments a player has participated in.
      tags:
        - Football
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique player identifier
          example: 831005
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/{id}/tournament/{tournamentId}/events/last/{page}:
    get:
      summary: Get player tournament last matches
      description: Retrieves a paginated list of the player's recent matches in a tournament.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique player identifier
          example: 831005
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 7
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number (0-based)
          example: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/player/trending:
    get:
      summary: Get trending players
      description: Retrieves a list of football players who are currently trending on SofaScore.
      tags:
        - Football
        - Players
      responses:
        '200':
          description: A JSON object containing a list of trending players.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/manager/{id}:
    get:
      summary: Get manager details
      description: Returns biographical and statistical information for a specific manager (coach).
      tags:
        - Football
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the manager
          example: 787526
      responses:
        '200':
          description: A JSON object containing detailed information about the manager.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/manager/{id}/image:
    get:
      summary: Get manager image
      description: Returns the profile image for the specified manager ID. The response is the raw image data.
      tags:
        - Football
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the manager
          example: 805670
      responses:
        '200':
          description: The manager's profile image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/manager/{id}/history:
    get:
      summary: Get manager career history
      description: Lists the teams previously and currently managed by a specific manager.
      tags:
        - Football
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the manager
          example: 787526
      responses:
        '200':
          description: A JSON object containing the manager's career history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/manager/{id}/matches/previous/{page}:
    get:
      summary: Get manager previous matches
      description: Fetches a paginated list of completed matches for a specific manager, sorted by most recent first.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the manager
          example: 794873
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the manager's previous matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/manager/{id}/matches/next/{page}:
    get:
      summary: Get manager upcoming matches
      description: Fetches a paginated list of scheduled future matches for a specific manager, sorted chronologically.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the manager
          example: 793676
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the manager's upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}:
    get:
      summary: Get league details
      description: Returns basic information about a tournament, including its name, sport, and current season details.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 16
      responses:
        '200':
          description: A JSON object containing detailed information about the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/image:
    get:
      summary: Get tournament logo
      description: Downloads the official logo or emblem for the specified tournament ID. The response is the raw image 
        data.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
      responses:
        '200':
          description: The tournament's logo image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/image/dark:
    get:
      summary: Get league dark logo
      description: Fetches the dark-theme variant of the tournament's logo. The response is the raw image data.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 679
      responses:
        '200':
          description: The tournament's dark logo image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/media:
    get:
      summary: Get league media
      description: Fetches a collection of media items (images, videos) related to the specified tournament.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
      responses:
        '200':
          description: A JSON object containing a list of media items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/seasons:
    get:
      summary: Get league seasons
      description: Lists every available season associated with the specified tournament.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 16
      responses:
        '200':
          description: A JSON object containing a list of seasons.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/standings/total:
    get:
      summary: Get total league standings
      description: Returns the complete league table (standings) with cumulative statistics for all teams in a season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing the complete standings table.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/standings/home:
    get:
      summary: Get home league standings
      description: Returns a league table calculated using only the results from games played at home during the season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing the home-only standings table.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/standings/away:
    get:
      summary: Get away league standings
      description: Returns a league table calculated solely from the results of away games during the season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing the away-only standings table.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/statistics:
    get:
      summary: Get aggregated player statistics
      description: Returns season-wide player performance metrics with optional filtering, ordering, and pagination.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 61627
        - name: limit
          in: query
          required: false
          schema:
            type: integer
          description: Number of results to return
          example: 20
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: Page number for pagination (defaults to 0)
          example: 0
        - name: group
          in: query
          required: false
          schema:
            type: string
          description: Grouping criterion
          example: goalkeeper
        - name: order
          in: query
          required: false
          schema:
            type: string
          description: Sorting order
          example: -saves
        - name: accumulation
          in: query
          required: false
          schema:
            type: string
          description: Accumulation mode
          example: total
        - name: minApps
          in: query
          required: false
          schema:
            type: integer
          description: Minimum appearances filter
        - name: fields
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated statistic fields to include
          example: ''
        - name: filters
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated filters.in.G'
          example: position
      responses:
        '200':
          description: A JSON object containing aggregated player statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/info:
    get:
      summary: Get tournament season info
      description: Provides metadata about a specific tournament season, such as the number of rounds and standings 
        format.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing information about the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/best-players:
    get:
      summary: Get overall league top players
      description: Provides a list of the top-performing players for a specified tournament season based on overall 
        ratings.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing a list of the top players.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/best-teams:
    get:
      summary: Get overall league top teams
      description: Returns a leaderboard of the top-performing teams based on aggregate statistics for a tournament 
        season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing a list of the top teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/best-players/per-game:
    get:
      summary: Get per-game player leaders
      description: Provides a list of players ranked by their per-game statistical averages across an entire tournament 
        season.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing players ranked by per-game averages.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/matches/last/{page}:
    get:
      summary: Get recent league matches
      description: Fetches a paginated list of past matches for a tournament season, ordered by most recent first.
      tags:
        - Football
        - Matches
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 76986
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the tournament's last matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/matches/next/{page}:
    get:
      summary: Get upcoming league matches
      description: Returns a paginated list of scheduled future matches for a tournament season, ordered 
        chronologically.
      tags:
        - Football
        - Matches
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 76986
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (must be >= 1)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the tournament's upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/tow/rounds:
    get:
      summary: List team of the week rounds
      description: Fetches the round numbers for which SofaScore has published a "Team of the Week" for a tournament 
        season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing a list of round numbers.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/rounds:
    get:
      summary: List league rounds
      description: Retrieves all round numbers (e.g., match weeks) defined for a specific tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing a list of rounds.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/round/{roundId}/team-of-week:
    get:
      summary: Get team of the week
      description: Returns the selected player lineup for the "Team of the Week" for a specific round of a tournament.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 61627
        - name: roundId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the round
          example: 19564
      responses:
        '200':
          description: A JSON object containing the Team of the Week lineup.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/groups:
    get:
      summary: Get league groups
      description: Fetches the group stage assignments and structure for a specific tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing group stage information.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/cuptrees:
    get:
      summary: Get league cup trees
      description: Fetches the knockout stage bracket (cup tree) for a specific tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 7
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76953
      responses:
        '200':
          description: A JSON object containing the knockout bracket.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/team-events/total:
    get:
      summary: Get league team events totals
      description: Returns cumulative team-level event counts (e.g., goals, cards) for a tournament season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing total team event statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/team-events/home:
    get:
      summary: Get league team home event totals
      description: Provides cumulative team-level statistics considering only home matches in a given season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing home-only team event statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/team-events/away:
    get:
      summary: Get league team away event totals
      description: Provides cumulative team-level statistics considering only away matches in a given season.
      tags:
        - Football
        - Teams
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing away-only team event statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/statistics/info:
    get:
      summary: Get player statistics metadata
      description: Retrieves information about the available statistic fields for players in a specific tournament 
        season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing metadata about available player statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/power-rankings/rounds:
    get:
      summary: List power ranking rounds
      description: Provides a list of round numbers for which power ranking data is available in a tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
      responses:
        '200':
          description: A JSON object containing a list of rounds with power rankings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/power-rankings/round/{round}:
    get:
      summary: Get power rankings by round
      description: Returns team power ranking positions and metrics for a selected round of a tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 16
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 41087
        - name: round
          in: path
          required: true
          schema:
            type: integer
          description: The round number
          example: 82
      responses:
        '200':
          description: A JSON object containing power rankings for the specified round.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/player-of-the-season:
    get:
      summary: Get player of the season
      description: Returns details about the officially awarded "Player of the Season" for a tournament.
      tags:
        - Football
        - Players
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 61643
      responses:
        '200':
          description: A JSON object containing details of the Player of the Season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/matches/round/{round}:
    get:
      summary: Get league events by round
      description: Lists all matches for a specific round of a tournament season.
      tags:
        - Football
        - Matches
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 17
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 76986
        - name: round
          in: path
          required: true
          schema:
            type: integer
          description: The round number
          example: 1
      responses:
        '200':
          description: A JSON object containing a list of matches for the round.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/matches/round/{round}/slug/{slug}:
    get:
      summary: Get league events by round and slug
      description: Lists matches for a specific round, further filtered by a stage slug (e.g., 'championship-round').
      tags:
        - Football
        - Matches
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 329
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 82988
        - name: round
          in: path
          required: true
          schema:
            type: integer
          description: The round number
          example: 1
        - name: slug
          in: path
          required: true
          schema:
            type: string
          description: The slug for the stage
          example: round-1
      responses:
        '200':
          description: A JSON object containing a list of matches for the round and slug.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/schedules/{day}/{month}/{year}:
    get:
      summary: Get tournament events on date
      description: Lists all events for a specific tournament scheduled on a given date, with optional timezone 
        formatting.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier of the tournament
          example: 270
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day component of the date
          example: 23
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month component of the date
          example: 12
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year component of the date
          example: 2025
        - name: timezone
          in: query
          required: false
          schema:
            type: string
          description: Optional timezone identifier
        - name: format
          in: query
          required: false
          schema:
            type: string
          description: Optional date format string
      responses:
        '200':
          description: A JSON object containing a list of scheduled events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/group/tournament/{tournamentId}/season/{seasonId}/matches:
    get:
      summary: Get tournament season events
      description: Returns a list of all events for a specified tournament and season, typically used for group stages.
      tags:
        - Football
        - Matches
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 1469
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 41897
      responses:
        '200':
          description: A JSON object containing a list of all events for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/group/tournament/{tournamentId}/season/{seasonId}/standings/total:
    get:
      summary: Get group tournament total standings
      description: Retrieves the total standings table for a tournament season in group context.
      tags:
        - Football
        - Tournaments
        - Standings
        - Groups
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 16752
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 69831
      responses:
        '200':
          description: The total standings table for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/group/tournament/{tournamentId}/season/{seasonId}/standings/home:
    get:
      summary: Get group tournament home standings
      description: Retrieves the home standings table for a tournament season in group context.
      tags:
        - Football
        - Tournaments
        - Standings
        - Groups
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the tournament
          example: 16752
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the season
          example: 69831
      responses:
        '200':
          description: The home standings table for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/featured-events:
    get:
      summary: Get tournament featured events
      description: Retrieves featured/highlighted events for a tournament.
      tags:
        - Football
        - Matches
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/winners:
    get:
      summary: Get tournament winners
      description: Retrieves historical winners of a tournament.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/player-news:
    get:
      summary: Get tournament player news
      description: Retrieves player-related news for a tournament.
      tags:
        - Football
        - Players
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/player-transfer-history/all/{page}:
    get:
      summary: Get tournament transfer history
      description: Retrieves paginated player transfer history for a tournament.
      tags:
        - Football
        - Players
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number (0-based)
          example: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/scheduled-events/{date}:
    get:
      summary: Get tournament events on date
      description: Retrieves all events for a tournament on a specific date.
      tags:
        - Football
        - Matches
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: date
          in: path
          required: true
          schema:
            type: string
          description: Date in YYYY-MM-DD format
          example: '2026-03-18'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/teams:
    get:
      summary: Get tournament season teams
      description: Retrieves all teams participating in a tournament season.
      tags:
        - Football
        - Teams
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/venues:
    get:
      summary: Get tournament season venues
      description: Retrieves all venues used in a tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/player-statistics/types:
    get:
      summary: Get player statistics info
      description: Retrieves available player statistic categories for a tournament season.
      tags:
        - Football
        - Players
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/team-statistics/types:
    get:
      summary: Get team statistics types
      description: Retrieves available team statistic categories for a tournament season.
      tags:
        - Football
        - Teams
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 76953
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/top-ratings/overall:
    get:
      summary: Get tournament top ratings
      description: Retrieves overall top-rated players for a tournament season.
      tags:
        - Football
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 7
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 76953
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/season/{seasonId}/player-of-the-season-race:
    get:
      summary: Get player of season race
      description: Retrieves the player of the season race standings for a tournament.
      tags:
        - Football
        - Players
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 7
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 76953
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tournament/{tournamentId}/top-player-performance:
    get:
      summary: Get tournament top player performance
      description: Retrieves top individual player performances for a tournament.
      tags:
        - Football
        - Players
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/referee/{id}:
    get:
      summary: Get referee details
      description: Returns the name, nationality, and career data for a specified referee.
      tags:
        - Football
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the referee
          example: 72926
      responses:
        '200':
          description: A JSON object containing detailed information about the referee.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/referee/{id}/image:
    get:
      summary: Get referee image
      description: Downloads the official photo for the specified referee ID. The response is the raw image data.
      tags:
        - Football
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the referee
          example: 787948
      responses:
        '200':
          description: The referee's image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/referee/{id}/statistics:
    get:
      summary: Get referee statistics
      description: Returns aggregated officiating data for a referee, such as total matches, cards issued, etc.
      tags:
        - Football
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the referee
          example: 72926
      responses:
        '200':
          description: A JSON object containing the referee's officiating statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/referee/{id}/matches/previous/{page}:
    get:
      summary: Get previous referee matches
      description: Fetches a paginated list of completed matches officiated by a referee, sorted by most recent first.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the referee
          example: 787948
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (zero-based)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the referee's previous matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/referee/{id}/matches/next/{page}:
    get:
      summary: Get upcoming referee matches
      description: Lists a paginated list of scheduled future matches assigned to a referee, sorted chronologically.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the referee
          example: 787948
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: The page number for pagination (zero-based)
          example: 0
      responses:
        '200':
          description: A paginated JSON object of the referee's upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/matches/odds/{day}/{month}/{year}:
    get:
      summary: Get sport odds by date
      description: Fetches pre-match betting odds for all football matches on a specific calendar date.
      tags:
        - Football
        - Matches
      parameters:
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 21
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A JSON object containing odds for matches on the specified date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/matches/odds/featured:
    get:
      summary: Get featured betting events
      description: Returns a curated list of highlighted football matches with their betting odds information.
      tags:
        - Football
        - Matches
      responses:
        '200':
          description: A JSON object containing a list of featured events with odds.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/category/{categoryId}/matches/{day}/{month}/{year}:
    get:
      summary: Get category scheduled events
      description: Lists all football matches for a given category occurring on a specific date (YYYY-MM-DD).
      tags:
        - Football
        - Matches
      parameters:
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
          description: Unique identifier for the category
          example: 1
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 15
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year in four-digit format
          example: 2025
      responses:
        '200':
          description: A JSON object containing a list of scheduled events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/venue/{id}:
    get:
      summary: Get venue details
      description: Retrieves detailed information for a specific venue.
      tags:
        - Football
        - Venues
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Venue identifier
          example: 709
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/venue/{id}/events/{sport}/{span}/{page}:
    get:
      summary: Get venue events
      description: Retrieves paginated events at a specific venue.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Venue identifier
          example: 709
        - name: sport
          in: path
          required: true
          schema:
            type: string
          description: Sport name filter
          example: all
        - name: span
          in: path
          required: true
          schema:
            type: string
          description: Time span (last or next)
          example: last
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number (0-based)
          example: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/venue/{id}/image:
    get:
      summary: Get venue image
      description: Retrieves the image for a specific venue.
      tags:
        - Football
        - Venues
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Venue identifier
          example: 709
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/venue/{id}/near-events:
    get:
      summary: Get venue near events
      description: Retrieves upcoming and recent events at a venue.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Venue identifier
          example: 709
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/jersey/away:
    get:
      summary: Get away player jersey
      description: Fetches the away jersey graphic for a player in a specific match. The response is the raw image data.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 10114139
      responses:
        '200':
          description: The away player jersey image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/goalkeeper/jersey/away:
    get:
      summary: Get away goalkeeper jersey
      description: Fetches the away jersey graphic for a goalkeeper in a specific match. The response is the raw image 
        data.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 10114139
      responses:
        '200':
          description: The away goalkeeper jersey image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/jersey/home:
    get:
      summary: Get home player jersey
      description: Fetches the home jersey graphic for a player in a specific match. The response is the raw image data.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 10114139
      responses:
        '200':
          description: The home player jersey image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/goalkeeper/jersey/home:
    get:
      summary: Get home goalkeeper jersey
      description: Fetches the home jersey graphic for a goalkeeper in a specific match. The response is the raw image 
        data.
      tags:
        - Football
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 10114139
      responses:
        '200':
          description: The home goalkeeper jersey image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/jersey/away-clean:
    get:
      summary: Get away player clean jersey
      description: Fetches the away clean jersey graphic for a player in a specific match. The response is the raw image
        data.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 14015387
      responses:
        '200':
          description: The away player clean jersey image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/match/{id}/player/jersey/home-clean:
    get:
      summary: Get home player clean jersey
      description: Fetches the home clean jersey graphic for a player in a specific match. The response is the raw image
        data.
      tags:
        - Football
        - Matches
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier of the match
          example: 14015387
      responses:
        '200':
          description: The home player clean jersey image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
components:
  schemas:
    JSONResult:
      type: object
      additionalProperties: true
      description: Generic JSON proxy payload.
    ErrorResponse:
      type: object
      additionalProperties: true
      description: Generic error payload.
