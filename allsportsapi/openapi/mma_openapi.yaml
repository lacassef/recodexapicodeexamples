openapi: 3.0.0
info:
  title: MMA API
  version: 1.0.0
  description: OpenAPI specification generated from SofaScore route annotations.
servers:
  - url: http://localhost:8080
    description: Local development server
paths:
  /api/tv/country/all/event/{id}:
    get:
      summary: Get match TV countries
      description: Lists the countries and the TV channels within them that are broadcasting a specific match.
      tags:
        - AllSports
        - TV
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the match
          example: 14081808
      responses:
        '200':
          description: A JSON object listing countries and their broadcasting channels.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/channel/{channId}/event/{id}:
    get:
      summary: Get match TV channel votes
      description: Fetches viewer voting data from a specific TV channel that is broadcasting the match.
      tags:
        - AllSports
        - TV
      parameters:
        - name: channId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the TV channel
          example: 3177
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the match
          example: 10974920
      responses:
        '200':
          description: A JSON object containing viewer vote data from the channel.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/img/flag/{flag}:
    get:
      summary: Get country flag
      description: Fetches the PNG flag image asset corresponding to a provided country code. The response is the raw 
        image data.
      tags:
        - AllSports
      parameters:
        - name: flag
          in: path
          required: true
          schema:
            type: string
          description: Country code or identifier
          example: jp
      responses:
        '200':
          description: The country's flag image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/country/{code}/flag:
    get:
      summary: Get country flag (img API)
      description: Fetches the flag image from the SofaScore image API by country code.
      tags:
        - AllSports
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          description: Country code or identifier
          example: AU
      responses:
        '200':
          description: The country's flag image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/tournament.png:
    get:
      summary: Get tournament placeholder image
      description: Returns a static placeholder graphic for a tournament. The response is an SVG image.
      tags:
        - AllSports
        - Tournaments
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/tournament.svg:
    get:
      summary: Get tournament placeholder image
      description: Returns a static placeholder graphic for a tournament. The response is an SVG image.
      tags:
        - AllSports
        - Tournaments
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/team.svg:
    get:
      summary: Get team placeholder image
      description: Returns a static placeholder graphic for a team. The response is an SVG image.
      tags:
        - AllSports
        - Teams
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/player.png:
    get:
      summary: Get player placeholder image
      description: Returns a static placeholder graphic for a player. The response is an SVG image.
      tags:
        - AllSports
        - Players
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/player.svg:
    get:
      summary: Get player placeholder image
      description: Returns a static placeholder graphic for a player. The response is an SVG image.
      tags:
        - AllSports
        - Players
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/manager.png:
    get:
      summary: Get manager placeholder image
      description: Returns a static placeholder graphic for a manager. The response is an SVG image.
      tags:
        - AllSports
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/manager.svg:
    get:
      summary: Get manager placeholder image
      description: Returns a static placeholder graphic for a manager. The response is an SVG image.
      tags:
        - AllSports
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholders/fighter-male.svg:
    get:
      summary: Get male fighter placeholder image
      description: Retrieves a placeholder image for a male fighter.
      tags:
        - AllSports
        - Placeholders
        - Media
      responses:
        '200':
          description: The placeholder SVG image.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholders/fighter-female.svg:
    get:
      summary: Get female fighter placeholder image
      description: Retrieves a placeholder image for a female fighter.
      tags:
        - AllSports
        - Placeholders
        - Media
      responses:
        '200':
          description: The placeholder SVG image.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/calendar/season/{seasonId}/{year}/days-with-events:
    get:
      summary: Get calendar days with events
      description: Retrieves days that have events for a specific season and year.
      tags:
        - Sofascore
        - Calendar
      parameters:
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Calendar year
          example: 2026
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/category/{categoryId}/image:
    get:
      summary: Get category image
      description: Retrieves the image/logo for a sport category.
      tags:
        - Sofascore
        - Categories
      parameters:
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
          description: Sport category identifier
          example: 1
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/countries/all:
    get:
      summary: Get all countries
      description: Retrieves a list of all available countries.
      tags:
        - Sofascore
        - Config
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/rankings/team/{teamId}:
    get:
      summary: Get team rankings
      description: Retrieves rankings for a specific team.
      tags:
        - Sofascore
        - Rankings
      parameters:
        - name: teamId
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/all:
    get:
      summary: Search all entities
      description: Searches across all entity types (teams, players, tournaments).
      tags:
        - Sofascore
        - Search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/suggestions/{entityType}:
    get:
      summary: Get search suggestions
      description: Retrieves search suggestions for a specific entity type.
      tags:
        - Sofascore
        - Search
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
          description: Entity type (teams, players, events, tournaments)
          example: teams
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/unique-tournaments:
    get:
      summary: Search unique tournaments
      description: Searches for unique tournaments.
      tags:
        - Sofascore
        - Search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/{entityType}:
    get:
      summary: Search by entity type
      description: Searches for entities of a specific type.
      tags:
        - Sofascore
        - Search
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
          description: Entity type (teams, players, events, tournaments)
          example: teams
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/channel/{id}/schedule:
    get:
      summary: Get TV channel schedule
      description: Retrieves the broadcast schedule for a specific TV channel.
      tags:
        - Sofascore
        - TV
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: TV channel identifier
          example: 3177
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/country/{countryCode}/channels:
    get:
      summary: Get country TV channels
      description: Retrieves available TV channels for a specific country.
      tags:
        - Sofascore
        - TV
      parameters:
        - name: countryCode
          in: path
          required: true
          schema:
            type: string
          description: ISO 3166-1 alpha-2 country code
          example: MZ
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/search/{term}:
    get:
      summary: Search MMA entities
      description: Searches for MMA fighters, events, and tournaments.
      tags:
        - MMA
        - Search
      parameters:
        - name: term
          in: path
          required: true
          schema:
            type: string
          description: Search term
          example: mcgregor
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: Page number for pagination (defaults to 0)
          example: 0
      responses:
        '200':
          description: A list of search results for fighters, events, and tournaments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid request parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/categories:
    get:
      summary: Get MMA categories
      description: Retrieves a list of all available MMA categories, which typically correspond to different 
        organizations (e.g., UFC, KSW).
      tags:
        - MMA
        - Categories
      responses:
        '200':
          description: A list of MMA categories.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/main-events/{date}/extended:
    get:
      summary: Get MMA main events extended by date
      description: Retrieves extended main events for MMA on a specific date.
      tags:
        - MMA
        - Events
      parameters:
        - name: date
          in: path
          required: true
          schema:
            type: string
          description: Date in YYYY-MM-DD format
          example: '2025-07-22'
      responses:
        '200':
          description: A list of extended main events for the date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/category/{id}/unique-tournaments:
    get:
      summary: Get unique tournaments by category
      description: Retrieves all unique MMA tournaments within a specific category (organization).
      tags:
        - MMA
        - Categories
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Category ID
          example: 1708
      responses:
        '200':
          description: A list of unique tournaments for the specified category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/main-events/last/{page}:
    get:
      summary: Get last main events for a tournament
      description: Retrieves a paginated list of the last main events for a specific MMA tournament.
      tags:
        - MMA
        - Tournaments
        - Events
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination
          example: 0
      responses:
        '200':
          description: A paginated list of the tournament's last main events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/main-events/next/{page}:
    get:
      summary: Get next main events for a tournament
      description: Retrieves a paginated list of the next main events for a specific MMA tournament.
      tags:
        - MMA
        - Tournaments
        - Events
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination
          example: 0
      responses:
        '200':
          description: A paginated list of the tournament's upcoming main events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}:
    get:
      summary: Get MMA tournament details
      description: Retrieves detailed information for a specific MMA tournament.
      tags:
        - MMA
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
      responses:
        '200':
          description: Detailed information about the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/rankings/unique-tournament/{tournamentId}/summary:
    get:
      summary: Get MMA rankings summary for a tournament
      description: Retrieves a summary of rankings for a specific MMA tournament.
      tags:
        - MMA
        - Tournaments
        - Rankings
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
      responses:
        '200':
          description: A summary of rankings for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/schedules/{day}/{month}/{year}:
    get:
      summary: Get MMA tournament schedule by date
      description: Retrieves the schedule of events for a specific MMA tournament on a given date.
      tags:
        - MMA
        - Tournaments
        - Events
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 15
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 9
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2024
      responses:
        '200':
          description: A list of scheduled events for the tournament on the given date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/schedules/{month}/{year}:
    get:
      summary: Get MMA scheduled main events by month
      description: Retrieves the main events scheduled for a specific MMA tournament in a given month and year.
      tags:
        - MMA
        - Tournaments
        - Events
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A list of scheduled main events for the tournament in the given month.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid month or year format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{uniqueTournamentId}/tournament/{tournamentId}/mma-events/all:
    get:
      summary: Get all MMA events for a tournament
      description: Retrieves all MMA events associated with a specific tournament and unique tournament ID.
      tags:
        - MMA
        - Tournaments
        - Events
      parameters:
        - name: uniqueTournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Unique Tournament ID
          example: 19905
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 154804
      responses:
        '200':
          description: A list of all MMA events for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/featured-events:
    get:
      summary: Get featured MMA events for a tournament
      description: Retrieves a list of featured MMA events for a specific tournament.
      tags:
        - MMA
        - Tournaments
        - Events
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
      responses:
        '200':
          description: A list of featured MMA events for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/image:
    get:
      summary: Get MMA tournament image
      description: Retrieves the logo image for a specific MMA tournament.
      tags:
        - MMA
        - Tournaments
        - Media
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19946
      responses:
        '200':
          description: The tournament's logo image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-tournament/{tournamentId}/image/dark:
    get:
      summary: Get MMA tournament dark-theme image
      description: Retrieves the dark-theme logo image for a specific MMA tournament.
      tags:
        - MMA
        - Tournaments
        - Media
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 19906
      responses:
        '200':
          description: The tournament's dark-theme logo image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{id}:
    get:
      summary: Get MMA event details by ID
      description: Retrieves detailed information for a specific MMA event by its ID.
      tags:
        - MMA
        - Events
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Event ID
          example: 13998594
      responses:
        '200':
          description: Detailed information about the event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{id}/meta:
    get:
      summary: Get MMA event metadata
      description: Retrieves metadata for a specific MMA event.
      tags:
        - MMA
        - Events
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Event ID
          example: 13998594
      responses:
        '200':
          description: Metadata for the event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{id}/odds/{providerId}/featured:
    get:
      summary: Get MMA event featured odds
      description: Retrieves featured odds for a specific MMA event and provider.
      tags:
        - MMA
        - Events
        - Odds
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Event ID
          example: 12140708
        - name: providerId
          in: path
          required: true
          schema:
            type: integer
          description: Provider identifier
          example: 1
      responses:
        '200':
          description: Featured odds for the event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{id}/pregame-form:
    get:
      summary: Get pre-game form for an MMA event
      description: Retrieves pre-game form and recent performance data for the fighters in a specific event.
      tags:
        - MMA
        - Events
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Event ID
          example: 13998594
      responses:
        '200':
          description: Pre-game form and performance trends for the fighters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{customId}/h2h/events:
    get:
      summary: Get head-to-head history for an MMA event
      description: Retrieves the detailed match history for head-to-head encounters between the two fighters in an 
        event.
      tags:
        - MMA
        - Events
        - Statistics
      parameters:
        - name: customId
          in: path
          required: true
          schema:
            type: string
          description: A custom identifier for the event, used for H2H lookups
          example: ItLdswYNd
      responses:
        '200':
          description: A detailed list of past H2H matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{id}/team-streaks:
    get:
      summary: Get fighter streaks for an event
      description: Provides recent form and streak information for each fighter in an event.
      tags:
        - MMA
        - Events
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Event ID
          example: 13998594
      responses:
        '200':
          description: Streak information for both fighters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/event/{id}/statistics:
    get:
      summary: Get MMA event statistics
      description: Retrieves comprehensive statistics for a specific MMA event.
      tags:
        - MMA
        - Events
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Event ID
          example: 12140708
      responses:
        '200':
          description: Comprehensive statistics for the event.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/team/{id}:
    get:
      summary: Get MMA fighter details
      description: Retrieves comprehensive details for a specific MMA fighter.
      tags:
        - MMA
        - Fighters
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Fighter (Team) ID
          example: 472421
      responses:
        '200':
          description: Comprehensive details for the fighter.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/team/{id}/career-statistics:
    get:
      summary: Get MMA fighter career statistics
      description: Retrieves the career statistics for a specific MMA fighter.
      tags:
        - MMA
        - Fighters
        - Statistics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Fighter (Team) ID
          example: 472421
      responses:
        '200':
          description: Career statistics for the fighter.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/team/{id}/events/last/{page}:
    get:
      summary: Get last events for an MMA fighter
      description: Retrieves a paginated list of the last events for a specific MMA fighter.
      tags:
        - MMA
        - Fighters
        - Events
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Fighter (Team) ID
          example: 472421
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination
          example: 0
      responses:
        '200':
          description: A paginated list of the fighter's last events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/team/{id}/events/next/{page}:
    get:
      summary: Get next events for an MMA fighter
      description: Retrieves a paginated list of the next events for a specific MMA fighter.
      tags:
        - MMA
        - Fighters
        - Events
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Fighter (Team) ID
          example: 472421
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination
          example: 0
      responses:
        '200':
          description: A paginated list of the fighter's upcoming events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/team/{id}/image:
    get:
      summary: Get MMA fighter image
      description: Retrieves the image for a specific MMA fighter.
      tags:
        - MMA
        - Fighters
        - Media
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Fighter (Team) ID
          example: 461600
      responses:
        '200':
          description: The fighter's image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/tournament/{tournamentId}:
    get:
      summary: Get MMA tournament details by ID
      description: Retrieves detailed information for a specific MMA tournament by its ID.
      tags:
        - MMA
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 154804
      responses:
        '200':
          description: Detailed information about the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/octogon.png:
    get:
      summary: Get MMA octagon image
      description: Retrieves the image of the MMA octagon.
      tags:
        - MMA
        - Media
      responses:
        '200':
          description: The MMA octagon image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/calendar/unique-tournament/{id}/{timezoneOffset}/months-with-events:
    get:
      summary: Get months with events
      description: Retrieves months that have events for a specific tournament.
      tags:
        - MMA
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Unique tournament identifier
          example: 8
        - name: timezoneOffset
          in: path
          required: true
          schema:
            type: integer
          description: Timezone offset
          example: 0
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/calendar/{sportSlug}/{date}/{countryId}/stages:
    get:
      summary: Get calendar sport stages
      description: Retrieves stage events for a sport on a specific date.
      tags:
        - MMA
        - Calendar
      parameters:
        - name: sportSlug
          in: path
          required: true
          schema:
            type: string
          description: Sport slug
          example: football
        - name: date
          in: path
          required: true
          schema:
            type: string
          description: Date in YYYY-MM-DD format
          example: '2026-03-18'
        - name: countryId
          in: path
          required: true
          schema:
            type: string
          description: Country identifier
          example: MZ
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/unique-stage/{id}/recent-stage-ids:
    get:
      summary: Get recent stage IDs
      description: Retrieves recent stage identifiers for a unique stage.
      tags:
        - MMA
        - Stages
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Stage identifier
          example: 214140
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/mma/stage/{stageId}/substages:
    get:
      summary: Get stage substages
      description: Retrieves substages (sessions/segments) for a motorsport stage.
      tags:
        - MMA
        - Stages
      parameters:
        - name: stageId
          in: path
          required: true
          schema:
            type: integer
          description: Stage identifier
          example: 214140
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
components:
  schemas:
    JSONResult:
      type: object
      additionalProperties: true
      description: Generic JSON proxy payload.
    ErrorResponse:
      type: object
      additionalProperties: true
      description: Generic error payload.
