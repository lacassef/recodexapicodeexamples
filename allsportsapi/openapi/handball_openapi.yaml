openapi: 3.0.0
info:
  title: Handball API
  version: 1.0.0
  description: OpenAPI specification generated from route annotations.
servers:
  - url: http://localhost:8080
    description: Local development server
paths:
  /api/tv/country/all/event/{id}:
    get:
      operationId: getMatchTVCountries
      summary: Get match TV countries
      description: Lists the countries and the TV channels within them that are broadcasting a specific match.
      tags:
        - TV
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the match
          example: 14081808
      responses:
        '200':
          description: A JSON object listing countries and their broadcasting channels.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/channel/{channId}/event/{id}:
    get:
      operationId: getMatchTVChannelVotes
      summary: Get match TV channel votes
      description: Fetches viewer voting data from a specific TV channel that is broadcasting the match.
      tags:
        - TV
      parameters:
        - name: channId
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the TV channel
          example: 3177
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: The unique identifier for the match
          example: 10974920
      responses:
        '200':
          description: A JSON object containing viewer vote data from the channel.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/img/flag/{flag}:
    get:
      operationId: getCountryFlag
      summary: Get country flag
      description: Fetches the PNG flag image asset corresponding to a provided country code. The response is the raw 
        image data.
      tags:
        - Media
      parameters:
        - name: flag
          in: path
          required: true
          schema:
            type: string
          description: Country code or identifier
          example: jp
      responses:
        '200':
          description: The country's flag image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/country/{code}/flag:
    get:
      operationId: getCountryFlagImgAPI
      summary: Get country flag (img API)
      description: Fetches the flag image from the image API by country code.
      tags:
        - Media
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          description: Country code or identifier
          example: AU
      responses:
        '200':
          description: The country's flag image file.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/tournament.png:
    get:
      operationId: getPlaceholderTournamentPng
      summary: Get tournament placeholder image
      description: Returns a static placeholder graphic for a tournament. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/tournament.svg:
    get:
      operationId: getPlaceholderTournamentSvg
      summary: Get tournament placeholder image
      description: Returns a static placeholder graphic for a tournament. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/team.svg:
    get:
      operationId: getTeamPlaceholderImage
      summary: Get team placeholder image
      description: Returns a static placeholder graphic for a team. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/player.png:
    get:
      operationId: getPlaceholderPlayerPng
      summary: Get player placeholder image
      description: Returns a static placeholder graphic for a player. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/player.svg:
    get:
      operationId: getPlaceholderPlayerSvg
      summary: Get player placeholder image
      description: Returns a static placeholder graphic for a player. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/manager.png:
    get:
      operationId: getPlaceholderManagerPng
      summary: Get manager placeholder image
      description: Returns a static placeholder graphic for a manager. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/placeholder/manager.svg:
    get:
      operationId: getPlaceholderManagerSvg
      summary: Get manager placeholder image
      description: Returns a static placeholder graphic for a manager. The response is an SVG image.
      tags:
        - Placeholders
      responses:
        '200':
          description: The placeholder SVG image file.
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/calendar/season/{seasonId}/{year}/days-with-events:
    get:
      operationId: getCalendarDaysWithEvents
      summary: Get calendar days with events
      description: Retrieves days that have events for a specific season and year.
      tags:
        - Calendar
      parameters:
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season identifier
          example: 61643
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Calendar year
          example: 2026
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/category/{categoryId}/image:
    get:
      operationId: getCategoryImage
      summary: Get category image
      description: Retrieves the image/logo for a sport category.
      tags:
        - Categories
      parameters:
        - name: categoryId
          in: path
          required: true
          schema:
            type: integer
          description: Sport category identifier
          example: 1
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/countries/all:
    get:
      operationId: getAllCountries
      summary: Get all countries
      description: Retrieves a list of all available countries.
      tags:
        - Config
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/rankings/team/{teamId}:
    get:
      operationId: getTeamRankings
      summary: Get team rankings
      description: Retrieves rankings for a specific team.
      tags:
        - Rankings
      parameters:
        - name: teamId
          in: path
          required: true
          schema:
            type: integer
          description: Unique team identifier
          example: 2817
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/all:
    get:
      operationId: searchAllEntities
      summary: Search all entities
      description: Searches across all entity types (teams, players, tournaments).
      tags:
        - Search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/suggestions/{entityType}:
    get:
      operationId: getSearchSuggestions
      summary: Get search suggestions
      description: Retrieves search suggestions for a specific entity type.
      tags:
        - Search
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
          description: Entity type (teams, players, events, tournaments)
          example: teams
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/unique-tournaments:
    get:
      operationId: searchUniqueTournaments
      summary: Search unique tournaments
      description: Searches for unique tournaments.
      tags:
        - Search
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/search/{entityType}:
    get:
      operationId: searchByEntityType
      summary: Search by entity type
      description: Searches for entities of a specific type.
      tags:
        - Search
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
          description: Entity type (teams, players, events, tournaments)
          example: teams
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/channel/{id}/schedule:
    get:
      operationId: getTVChannelSchedule
      summary: Get TV channel schedule
      description: Retrieves the broadcast schedule for a specific TV channel.
      tags:
        - TV
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: TV channel identifier
          example: 3177
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/tv/country/{countryCode}/channels:
    get:
      operationId: getCountryTVChannels
      summary: Get country TV channels
      description: Retrieves available TV channels for a specific country.
      tags:
        - TV
      parameters:
        - name: countryCode
          in: path
          required: true
          schema:
            type: string
          description: ISO 3166-1 alpha-2 country code
          example: MZ
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/search/{term}:
    get:
      operationId: searchHandballEntities
      summary: Search handball entities
      description: Searches for handball teams, players, and tournaments.
      tags:
        - Search
      parameters:
        - name: term
          in: path
          required: true
          schema:
            type: string
          description: Search term
          example: asobal
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: Page number for pagination (defaults to 0)
          example: 0
      responses:
        '200':
          description: A list of search results for teams, players, and tournaments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid request parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/category/{id}/events/{day}/{month}/{year}:
    get:
      operationId: getHandballMatchesByCategoryAndDate
      summary: Get handball matches by category and date
      description: Retrieves all handball matches for a specific category on a given date.
      tags:
        - Categories
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Category ID
          example: 71
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month (1-31)
          example: 24
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month (1-12)
          example: 5
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2023
      responses:
        '200':
          description: A list of scheduled matches for the category on the given date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/matches/live:
    get:
      operationId: getLiveHandballMatches
      summary: Get live handball matches
      description: Retrieves all currently live handball matches.
      tags:
        - Matches
      responses:
        '200':
          description: A list of live handball matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/tournament/categories:
    get:
      operationId: getHandballTournamentCategories
      summary: Get handball tournament categories
      description: Retrieves all available handball tournament categories (countries/regions).
      tags:
        - Tournaments
      responses:
        '200':
          description: A list of tournament categories.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/all/category/{id}:
    get:
      operationId: getTournamentsByCategory
      summary: Get tournaments by category
      description: Retrieves all handball tournaments within a specific category.
      tags:
        - Tournaments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Category ID
          example: 71
      responses:
        '200':
          description: A list of tournaments for the specified category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/matches/{day}/{month}/{year}:
    get:
      operationId: getHandballMatchesByDate
      deprecated: true
      summary: Get handball matches by date
      description: Deprecated — the flat per-date feed is being retired. Assemble the day per competition instead — list categories then call /api/{sport}/category/{id}/events/{day}/{month}/{year}, or per tournament /api/{sport}/tournament/{id}/scheduled-events/{date} (ISO date). See the AllSportsAPI FAQ on getting all matches for a specific date. Fetches all handball matches scheduled for a specific date.
      tags:
        - Matches
      parameters:
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month (1-31)
          example: 13
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month (1-12)
          example: 5
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2023
      responses:
        '200':
          description: A list of scheduled matches for the given date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}:
    get:
      operationId: getHandballMatchDetails
      summary: Get handball match details
      description: Retrieves detailed information about a specific handball match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485769
      responses:
        '200':
          description: Detailed information about the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/odds:
    get:
      operationId: getMatchBettingOdds
      summary: Get match betting odds
      description: Retrieves betting odds from various bookmakers for the handball match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485769
      responses:
        '200':
          description: A list of available betting odds for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/incidents:
    get:
      operationId: getMatchIncidents
      summary: Get match incidents
      description: Retrieves all incidents and events that occurred during the match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485760
      responses:
        '200':
          description: A list of incidents from the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/best-players:
    get:
      operationId: getMatchBestPlayers
      summary: Get match best players
      description: Retrieves the best performing players from the match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485760
      responses:
        '200':
          description: A list of the best performing players in the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/votes:
    get:
      operationId: getMatchVotes
      summary: Get match votes
      description: Retrieves fan voting results for the match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485760
      responses:
        '200':
          description: Fan voting results for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/duel:
    get:
      operationId: getHeadToHeadSummary
      summary: Get head-to-head summary
      description: Retrieves a summary of head-to-head statistics between the teams.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485760
      responses:
        '200':
          description: A summary of H2H statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/form:
    get:
      operationId: getMatchFormAnalysis
      summary: Get match form analysis
      description: Retrieves recent form and performance trends for teams in the match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485760
      responses:
        '200':
          description: Pregame form and performance trends for the teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/lineups:
    get:
      operationId: getMatchLineups
      summary: Get match lineups
      description: Retrieves starting lineups and player positions for both teams.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485769
      responses:
        '200':
          description: Starting lineups and substitutes for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/statistics:
    get:
      operationId: getMatchStatistics
      summary: Get match statistics
      description: Retrieves comprehensive statistical data for the handball match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485769
      responses:
        '200':
          description: Comprehensive statistics for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/streaks:
    get:
      operationId: getMatchStreaks
      summary: Get match streaks
      description: Retrieves winning/scoring streaks information for the match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 10485769
      responses:
        '200':
          description: Streak information for both teams.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{customId}/h2h:
    get:
      operationId: getHeadToHeadHistory
      summary: Get head-to-head history
      description: Retrieves detailed head-to-head match history between the teams.
      tags:
        - Matches
      parameters:
        - name: customId
          in: path
          required: true
          schema:
            type: string
          description: Custom match identifier
          example: tAcsmjd
      responses:
        '200':
          description: A detailed list of past H2H matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/matches/previous/{page}:
    get:
      operationId: getPreviousTeamMatches
      summary: Get previous team matches
      description: Fetches a list of completed matches for the team, sorted by date descending.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination (starts from 0)
          example: 0
      responses:
        '200':
          description: A paginated list of the team's previous matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number or team ID format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/matches/next/{page}:
    get:
      operationId: getUpcomingTeamMatches
      summary: Get upcoming team matches
      description: Fetches a list of scheduled matches for the team, sorted by date ascending.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination (starts from 0)
          example: 0
      responses:
        '200':
          description: A paginated list of the team's upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number or team ID format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}:
    get:
      operationId: getHandballTeamDetails
      summary: Get handball team details
      description: Retrieves comprehensive information about a handball team.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: Comprehensive details for the team.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/players:
    get:
      operationId: getTeamPlayers
      summary: Get team players
      description: Retrieves the current roster of players for the team.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: The current roster of players for the team.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/matches/near:
    get:
      operationId: getTeamNearMatches
      summary: Get team near matches
      description: Retrieves recent and upcoming matches for the team.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: A list of recent and upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/unique-tournaments:
    get:
      operationId: getTeamTournaments
      summary: Get team tournaments
      description: Retrieves all tournaments the team participates in.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: A list of unique tournaments the team has participated in.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/standings/seasons:
    get:
      operationId: getTeamStandingsSeasons
      summary: Get team standings seasons
      description: Retrieves available seasons for team standings data.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: A list of seasons with available standings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/player-statistics/seasons:
    get:
      operationId: getTeamPlayerStatisticsSeasons
      summary: Get team player statistics seasons
      description: Retrieves available seasons for team player statistics.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: A list of seasons with available player statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/unique-tournament/{tournamentId}/season/{seasonId}/top-players/overall:
    get:
      operationId: getTeamTopPlayersOverall
      summary: Get team top players overall
      description: Retrieves the best performing players for the team in a specific tournament season.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: A list of the team's top players for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/standings/total:
    get:
      operationId: getTournamentTotalStandings
      summary: Get tournament total standings
      description: Retrieves complete standings table for all teams in the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: The complete standings table for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/standings/home:
    get:
      operationId: getTournamentHomeStandings
      summary: Get tournament home standings
      description: Retrieves standings based on home games only.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: The home standings table for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/standings/away:
    get:
      operationId: getTournamentAwayStandings
      summary: Get tournament away standings
      description: Retrieves standings based on away games only.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: The away standings table for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}:
    get:
      operationId: getTournamentDetails
      summary: Get tournament details
      description: Retrieves comprehensive information about a handball tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
      responses:
        '200':
          description: Comprehensive details for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/seasons:
    get:
      operationId: getTournamentSeasons
      summary: Get tournament seasons
      description: Retrieves all available seasons for the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
      responses:
        '200':
          description: A list of available seasons for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/player-statistics/types:
    get:
      operationId: getPlayerStatisticsTypes
      summary: Get player statistics types
      description: Retrieves available player statistics categories for the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: A list of available player statistics types.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/top-players/overall:
    get:
      operationId: getTopPlayersOverall
      summary: Get top players overall
      description: Retrieves the best performing players for the entire season.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: A list of the top performing players for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/top-players-per-game/all/overall:
    get:
      operationId: getTopPlayersPerGame
      summary: Get top players per game
      description: Retrieves the best performing players based on per-game averages.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: Player rankings based on per-game averages.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/top-teams/overall:
    get:
      operationId: getTopTeamsOverall
      summary: Get top teams overall
      description: Retrieves the best performing teams for the entire season.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: A list of the top performing teams for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/team-events/total:
    get:
      operationId: getTeamEventsTotalStatistics
      summary: Get team events total statistics
      description: Retrieves aggregated team performance statistics for all games.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: Aggregated team statistics for the entire season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/team-events/home:
    get:
      operationId: getTeamEventsHomeStatistics
      summary: Get team events home statistics
      description: Retrieves aggregated team performance statistics for home games.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: Aggregated team statistics for home games.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/team-events/away:
    get:
      operationId: getTeamEventsAwayStatistics
      summary: Get team events away statistics
      description: Retrieves aggregated team performance statistics for away games.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: Aggregated team statistics for away games.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/matches/last/{page}:
    get:
      operationId: getTournamentLastMatches
      summary: Get tournament last matches
      description: Retrieves recently played matches in the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination (starts from 0)
          example: 0
      responses:
        '200':
          description: A paginated list of the tournament's last matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/matches/next/{page}:
    get:
      operationId: getTournamentUpcomingMatches
      summary: Get tournament upcoming matches
      description: Retrieves scheduled upcoming matches in the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 78525
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination (starts from 0)
          example: 0
      responses:
        '200':
          description: A paginated list of the tournament's upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/player/{id}:
    get:
      operationId: getHandballPlayerDetails
      summary: Get handball player details
      description: Retrieves comprehensive information about a handball player.
      tags:
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Player ID
          example: 944907
      responses:
        '200':
          description: Comprehensive details for the player.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/player/{id}/matches/near:
    get:
      operationId: getPlayerNearMatches
      summary: Get player near matches
      description: Retrieves recent and upcoming matches for the player.
      tags:
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Player ID
          example: 944907
      responses:
        '200':
          description: A list of the player's recent and upcoming matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/player/{id}/matches/previous/{page}:
    get:
      operationId: getPreviousPlayerMatches
      summary: Get previous player matches
      description: Fetches a list of completed matches for the player, sorted by date descending.
      tags:
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Player ID
          example: 944907
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number for pagination (starts from 0)
          example: 0
      responses:
        '200':
          description: A paginated list of the player's previous matches.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid page number or player ID format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/player/{id}/statistics/season:
    get:
      operationId: getPlayerStatisticsSeasons
      summary: Get player statistics seasons
      description: Retrieves available seasons for player statistics.
      tags:
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Player ID
          example: 944907
      responses:
        '200':
          description: A list of seasons with available statistics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/player/{id}/unique-tournament/{tournamentId}/season/{seasonId}/statistics/overall:
    get:
      operationId: getPlayerOverallStatistics
      summary: Get player overall statistics
      description: Retrieves player's overall statistics for a specific tournament season.
      tags:
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Player ID
          example: 944907
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42967
      responses:
        '200':
          description: The player's overall statistics for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/cuptrees:
    get:
      operationId: getTournamentCupTrees
      summary: Get tournament cup trees
      description: Retrieves playoff/knockout bracket structure for the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 160
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 24048
      responses:
        '200':
          description: The knockout bracket for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/media:
    get:
      operationId: getTournamentMedia
      summary: Get tournament media
      description: Retrieves media content for the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 30
      responses:
        '200':
          description: A list of media content for the tournament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/tournament/{tournamentId}/season/{seasonId}/matches:
    get:
      operationId: getTournamentSeasonMatches
      summary: Get tournament season matches
      description: Retrieves all matches for a tournament season.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 2465
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42851
      responses:
        '200':
          description: A list of all events for the season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/groups:
    get:
      operationId: getTournamentGroups
      summary: Get tournament groups
      description: Retrieves group stage information for the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 30
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42851
      responses:
        '200':
          description: Group stage information for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/rounds:
    get:
      operationId: getTournamentRounds
      summary: Get tournament rounds
      description: Retrieves all rounds/game weeks for the tournament season.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 30
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42851
      responses:
        '200':
          description: A list of rounds for the tournament season.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/matches/round/{round}:
    get:
      operationId: getTournamentMatchesByRound
      summary: Get tournament matches by round
      description: Retrieves matches for a specific round in the tournament.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 30
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42851
        - name: round
          in: path
          required: true
          schema:
            type: integer
          description: Round number
          example: 3
      responses:
        '200':
          description: A list of matches for the specified round.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/season/{seasonId}/matches/round/{round}/slug/{slug}:
    get:
      operationId: getTournamentMatchesByRoundWithSlug
      summary: Get tournament matches by round with slug
      description: Retrieves matches for a specific round with tournament slug.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 30
        - name: seasonId
          in: path
          required: true
          schema:
            type: integer
          description: Season ID
          example: 42851
        - name: round
          in: path
          required: true
          schema:
            type: integer
          description: Round number
          example: 27
        - name: slug
          in: path
          required: true
          schema:
            type: string
          description: Tournament slug
          example: quarterfinal
      responses:
        '200':
          description: A list of matches for the specified round and slug.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/match/{id}/highlights:
    get:
      operationId: getMatchHighlights
      summary: Get match highlights
      description: Retrieves video highlights and key moments from the match.
      tags:
        - Matches
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Match ID
          example: 14211254
      responses:
        '200':
          description: A list of video highlights for the match.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/image:
    get:
      operationId: getTournamentLogo
      summary: Get tournament logo
      description: Retrieves the tournament's logo image.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
      responses:
        '200':
          description: Tournament logo image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/unique-tournament/{tournamentId}/image/dark:
    get:
      operationId: getTournamentDarkLogo
      summary: Get tournament dark logo
      description: Retrieves the tournament's dark theme logo image.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
      responses:
        '200':
          description: Tournament dark logo image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/team/{id}/image:
    get:
      operationId: getTeamLogo
      summary: Get team logo
      description: Retrieves the team's logo image.
      tags:
        - Teams
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Team ID
          example: 6272
      responses:
        '200':
          description: Team logo image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/player/{id}/image:
    get:
      operationId: getPlayerImage
      summary: Get player image
      description: Retrieves the player's profile image.
      tags:
        - Players
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          description: Player ID
          example: 944907
      responses:
        '200':
          description: Player profile image.
          content:
            image/png:
              schema:
                type: string
                format: binary
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/tournament/{tournamentId}/schedules/{day}/{month}/{year}:
    get:
      operationId: getTournamentMatchesByDate
      summary: Get tournament matches by date
      description: Retrieves all tournament matches scheduled for a specific date.
      tags:
        - Tournaments
      parameters:
        - name: tournamentId
          in: path
          required: true
          schema:
            type: integer
          description: Tournament ID
          example: 149
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month (1-31)
          example: 23
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month (1-12)
          example: 12
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
      responses:
        '200':
          description: A list of scheduled matches for the tournament on the given date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters 
            (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
  /api/handball/scheduled-tournaments/{day}/{month}/{year}/page/{page}:
    get:
      operationId: getHandballScheduledTournamentsByDate
      summary: Get handball scheduled tournaments by date
      description: Retrieves a paginated list of tournaments that have scheduled handball matches for a specific date.
      tags:
        - Tournaments
      parameters:
        - name: day
          in: path
          required: true
          schema:
            type: integer
          description: Day of the month
          example: 22
        - name: month
          in: path
          required: true
          schema:
            type: integer
          description: Month of the year
          example: 7
        - name: year
          in: path
          required: true
          schema:
            type: integer
          description: Year
          example: 2025
        - name: page
          in: path
          required: true
          schema:
            type: integer
          description: Page number (1-based)
          example: 1
      responses:
        '200':
          description: A JSON object containing a paginated list of scheduled tournaments for the date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JSONResult'
        '400':
          description: Invalid date format in path.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Internal server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '204':
          description: No content. The requested route exists but there is no data available for the given parameters (e.g. match has no TV coverage, no statistics yet, empty search results, etc.).
components:
  schemas:
    JSONResult:
      type: object
      additionalProperties: true
      description: Generic JSON proxy payload.
    ErrorResponse:
      type: object
      additionalProperties: true
      description: Generic error payload.
